// import type { AppError } from '../types';
// type context = Record<string, unknown>;

// export const logError = (err: AppError, context?: context) => {
//   console.error({
//     ...context,
//     name: err.name,
//     message: err.message,
//     kind: err.kind,
//     code: err.code,
//     metadata: err.metadata,
//   });

//   // TODO: send to remote service (Sentry, Honeycomb), but keep non-blocking
// };
