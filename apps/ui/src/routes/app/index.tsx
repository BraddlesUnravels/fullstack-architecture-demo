import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <section id="applications-home" class="flex h-full w-full items-center justify-center">
      <h1 class="font-extrabold text-4xl">Home</h1>
    </section>
  );
});
