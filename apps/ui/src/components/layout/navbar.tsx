import { component$ } from '@builder.io/qwik';

export const NavBar = component$(() => {
  // TODO - Build and add navbar components
  return (
    <div class="navbar bg-base-100 shadow-sm">
      <>Place Navbar Content Here</>
    </div>
  );
});
