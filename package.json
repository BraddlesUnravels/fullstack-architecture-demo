{
  "name": "job-application-tracker",
  "version": "0.1.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.3.13",
    "bun-types": "^1.3.13",
    "eslint": "^10.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "typescript-eslint": "^8.46.2",
    "typescript": "^6.0.3"
  },
  "lint-staged": {
    "*.{js,css,md,ts,tailwindcss,tsx}": "prettier --write"
  },
  "private": false,
  "scripts": {
    "dev:ui": "bun run --filter='./apps/*' dev",
    "dev:db:init": "bun run --filter='./packages/db init",
    "dev:db": "bun run --filter='./packages/* hot:reset",
    "dev:api": "bun run --filter='./services/*' dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
}
